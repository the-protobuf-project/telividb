/** A collection, as the window needs it. */

/** One collection the engine holds. */
export interface CollectionSummary {
  /** The id within the server, which every other call takes. */
  id: string;
}
