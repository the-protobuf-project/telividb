.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Storybook's built output — a build artifact, reproducible from the stories.
storybook-static/

# Screenshots taken by `bun run shot` — a working aid for comparing the app
# against the mock, never an artefact worth committing.
.shots/
