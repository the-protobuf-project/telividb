// @generated
// Code generated by protoc-gen-mcp. DO NOT EDIT.
// versions:
// 	protoc-gen-mcp v1.7.1
// Authors: The Protobuf Project - Open Source 2026
// source: telividb/inference/v1/inference_service.proto

use async_trait::async_trait;
use rmcp::{
    ErrorData as McpError, RoleServer, ServerHandler, ServiceExt,
    model::*,
    service::{Peer, RequestContext},
};
use serde_json::{self, Value, json};
use std::sync::Arc;

fn make_tool(
    name: &str,
    description: &str,
    schema_json: &str,
    output_schema_json: &str,
    annotations: Value,
    title: &str,
    icons: Value,
) -> Tool {
    let mut tool = json!({
        "name": name, "description": description,
        "inputSchema": serde_json::from_str::<Value>(schema_json).unwrap(),
        "outputSchema": serde_json::from_str::<Value>(output_schema_json).unwrap(),
    });
    // A hint the proto does not state stays absent: to a client, "unknown" is
    // not the same as "false".
    if !annotations.is_null() {
        tool["annotations"] = annotations;
    }
    if !title.is_empty() {
        tool["title"] = json!(title);
    }
    if !icons.is_null() {
        tool["icons"] = icons;
    }
    serde_json::from_value(tool).expect("generated tool schema must be valid")
}

fn make_tool_with_app_meta(
    name: &str,
    description: &str,
    schema_json: &str,
    output_schema_json: &str,
    annotations: Value,
    title: &str,
    icons: Value,
    app_resource_uri: &str,
) -> Tool {
    let mut tool = json!({
        "name": name, "description": description,
        "inputSchema": serde_json::from_str::<Value>(schema_json).unwrap(),
        "outputSchema": serde_json::from_str::<Value>(output_schema_json).unwrap(),
        "_meta": { "ui": { "resourceUri": app_resource_uri } }
    });
    if !annotations.is_null() {
        tool["annotations"] = annotations;
    }
    if !title.is_empty() {
        tool["title"] = json!(title);
    }
    if !icons.is_null() {
        tool["icons"] = icons;
    }
    serde_json::from_value(tool).expect("generated tool schema must be valid")
}

fn app_resource_uri(service_name: &str) -> String {
    format!("ui://{}/app.html", service_name.to_lowercase())
}

/// Sends MCP progress notifications for one tool call.
///
/// A server-streaming RPC reports incremental progress through this. When the
/// client sent no progressToken there is nothing to correlate a notification
/// with, so every send is a no-op and an implementation never has to check.
///
/// The `Default` sink is exactly that inert one, which is what a unit test for a
/// streaming RPC wants.
#[derive(Clone, Default)]
pub struct McpProgressSink {
    peer: Option<Peer<RoleServer>>,
    token: Option<ProgressToken>,
}

impl McpProgressSink {
    fn new(peer: Option<Peer<RoleServer>>, token: Option<ProgressToken>) -> Self {
        Self { peer, token }
    }

    /// Reports progress. `total` is the expected final value when known.
    pub async fn send(&self, progress: f64, total: Option<f64>, message: Option<String>) {
        let (Some(peer), Some(token)) = (self.peer.as_ref(), self.token.as_ref()) else {
            return;
        };
        // ProgressNotificationParam is #[non_exhaustive], so it is built through
        // its constructor rather than a struct literal.
        let mut param = ProgressNotificationParam::new(token.clone(), progress);
        if let Some(total) = total {
            param = param.with_total(total);
        }
        if let Some(message) = message {
            param = param.with_message(message);
        }
        // A dropped client should not fail the tool call it is no longer watching.
        let _ = peer.notify_progress(param).await;
    }
}

fn default_app_html(app_name: &str, version: &str, description: &str) -> String {
    format!(
        "<!DOCTYPE html><html lang=\"en\"><head><meta charset=\"utf-8\"><title>{app_name}</title></head><body><h1>{app_name}</h1><p>v{version}</p><p>{description}</p><p>This is a generated MCP App placeholder. Replace this resource with your own UI.</p></body></html>"
    )
}

const INFERENCE__EMBED_TEXT_SCHEMA_JSON: &str = r##"{"description":"Computes embeddings for a batch of text. Policy is evaluated before the model runs, not only before results return. That is stronger than checking at the query planner alone: it stops a caller producing a vector for data it was never granted, rather than hiding the output afterwards.","properties":{"input":{"items":{"type":"string"},"type":"array"},"model":{"type":"string"},"task":{"enum":["TASK_UNSPECIFIED","TASK_DOCUMENT","TASK_QUERY"],"type":"string"},"truncate":{"type":"boolean"}},"required":["model","input"],"type":"object"}"##;
const INFERENCE__EMBED_TEXT_OUTPUT_SCHEMA_JSON: &str = r##"{"properties":{"embeddings":{"items":{"properties":{"data":{"contentEncoding":"base64","format":"byte","type":"string"},"dimensions":{"type":"integer"}},"required":["data","dimensions"],"type":"object"},"type":"array"},"load_duration":{"pattern":"^-?[0-9]+(\\.[0-9]+)?s$","type":["string","null"]},"model_digest":{"type":"string"},"prompt_token_count":{"type":"integer"}},"required":[],"type":"object"}"##;
const INFERENCE__GET_MODEL_SCHEMA_JSON: &str = r##"{"description":"Retrieves a single model.","properties":{"name":{"type":"string"}},"required":["name"],"type":"object"}"##;
const INFERENCE__GET_MODEL_OUTPUT_SCHEMA_JSON: &str = r##"{"properties":{"architecture":{"type":"string"},"context_length":{"type":"integer"},"create_time":{"format":"date-time","type":["string","null"]},"digest":{"type":"string"},"dimensions":{"type":"integer"},"etag":{"type":"string"},"name":{"type":"string"},"quantization":{"type":"string"},"resident":{"type":"boolean"},"resident_bytes":{"type":"string"}},"required":[],"type":"object"}"##;
const INFERENCE__LIST_MODELS_SCHEMA_JSON: &str = r##"{"description":"Lists models known to the inference server.","properties":{"filter":{"type":"string"},"page_size":{"type":"integer"},"page_token":{"type":"string"}},"required":[],"type":"object"}"##;
const INFERENCE__LIST_MODELS_OUTPUT_SCHEMA_JSON: &str = r##"{"properties":{"models":{"items":{"properties":{"architecture":{"type":"string"},"context_length":{"type":"integer"},"create_time":{"format":"date-time","type":["string","null"]},"digest":{"type":"string"},"dimensions":{"type":"integer"},"etag":{"type":"string"},"name":{"type":"string"},"quantization":{"type":"string"},"resident":{"type":"boolean"},"resident_bytes":{"type":"string"}},"required":[],"type":"object"},"type":"array"},"next_page_token":{"type":"string"}},"required":[],"type":"object"}"##;

#[async_trait]
pub trait InferenceMcpServer: Send + Sync + 'static {
    async fn embed_text(&self, args: Value) -> std::result::Result<Value, McpError>;
    async fn get_model(&self, args: Value) -> std::result::Result<Value, McpError>;
    async fn list_models(&self, args: Value) -> std::result::Result<Value, McpError>;
}

pub struct InferenceMcpHandler<T: InferenceMcpServer> {
    inner: Arc<T>,
}

impl<T: InferenceMcpServer> Clone for InferenceMcpHandler<T> {
    fn clone(&self) -> Self {
        Self {
            inner: Arc::clone(&self.inner),
        }
    }
}

impl<T: InferenceMcpServer> InferenceMcpHandler<T> {
    pub fn new(svc: T) -> Self {
        Self {
            inner: Arc::new(svc),
        }
    }

    pub fn tools() -> Vec<Tool> {
        let app_uri = app_resource_uri("Inference");
        vec![
            make_tool_with_app_meta(
                "inference-embed_text_v1",
                "Computes embeddings for a batch of text. Policy is evaluated before the model runs, not only before results return. That is stronger than checking at the query planner alone: it stops a caller producing a vector for data it was never granted, rather than hiding the output afterwards.",
                INFERENCE__EMBED_TEXT_SCHEMA_JSON,
                INFERENCE__EMBED_TEXT_OUTPUT_SCHEMA_JSON,
                Value::Null,
                "",
                Value::Null,
                &app_uri,
            ),
            make_tool_with_app_meta(
                "inference-get_model_v1",
                "Retrieves a single model.",
                INFERENCE__GET_MODEL_SCHEMA_JSON,
                INFERENCE__GET_MODEL_OUTPUT_SCHEMA_JSON,
                Value::Null,
                "",
                Value::Null,
                &app_uri,
            ),
            make_tool_with_app_meta(
                "inference-list_models_v1",
                "Lists models known to the inference server.",
                INFERENCE__LIST_MODELS_SCHEMA_JSON,
                INFERENCE__LIST_MODELS_OUTPUT_SCHEMA_JSON,
                Value::Null,
                "",
                Value::Null,
                &app_uri,
            ),
        ]
    }

    pub fn resources() -> Vec<Resource> {
        vec![
            serde_json::from_value(json!({
                "uri": app_resource_uri("Inference"),
                "name": "telividb inference",
                "mimeType": "text/html"
            }))
            .expect("generated app resource must be valid"),
        ]
    }

    pub fn resource_templates() -> Vec<ResourceTemplate> {
        vec![
            serde_json::from_value(json!({
                "uriTemplate": "model://models/{model}", "name": "Model",
                "description": "Model resource (models/{model})", "mimeType": "application/json"
            }))
            .expect("generated resource template must be valid"),
        ]
    }
}

impl<T: InferenceMcpServer> ServerHandler for InferenceMcpHandler<T> {
    fn get_info(&self) -> ServerInfo {
        ServerInfo::new(
            ServerCapabilities::builder()
                .enable_tools()
                .enable_resources()
                .build(),
        )
        .with_server_info(Implementation::new("Inference", "0.1.0"))
    }

    async fn list_tools(
        &self,
        _: Option<PaginatedRequestParams>,
        _: RequestContext<RoleServer>,
    ) -> std::result::Result<ListToolsResult, McpError> {
        Ok(ListToolsResult::with_all_items(Self::tools()))
    }
    async fn call_tool(
        &self,
        request: CallToolRequestParams,
        _context: RequestContext<RoleServer>,
    ) -> std::result::Result<CallToolResponse, McpError> {
        let args = request
            .arguments
            .map_or_else(|| Value::Object(Default::default()), Value::Object);
        match request.name.as_ref() {
            "inference-embed_text_v1" => {
                let result = self.inner.embed_text(args).await?;
                let text = serde_json::to_string(&result).map_err(|e| {
                    McpError::internal_error(format!("serialize response: {e}"), None)
                })?;
                Ok(CallToolResult::success(vec![ContentBlock::text(text)]).into())
            }
            "inference-get_model_v1" => {
                let result = self.inner.get_model(args).await?;
                let text = serde_json::to_string(&result).map_err(|e| {
                    McpError::internal_error(format!("serialize response: {e}"), None)
                })?;
                Ok(CallToolResult::success(vec![ContentBlock::text(text)]).into())
            }
            "inference-list_models_v1" => {
                let result = self.inner.list_models(args).await?;
                let text = serde_json::to_string(&result).map_err(|e| {
                    McpError::internal_error(format!("serialize response: {e}"), None)
                })?;
                Ok(CallToolResult::success(vec![ContentBlock::text(text)]).into())
            }
            _ => Err(McpError::internal_error(
                format!("unknown tool: {}", request.name),
                None,
            )),
        }
    }

    async fn list_resources(
        &self,
        _: Option<PaginatedRequestParams>,
        _: RequestContext<RoleServer>,
    ) -> std::result::Result<ListResourcesResult, McpError> {
        Ok(ListResourcesResult::with_all_items(Self::resources()))
    }

    async fn list_resource_templates(
        &self,
        _: Option<PaginatedRequestParams>,
        _: RequestContext<RoleServer>,
    ) -> std::result::Result<ListResourceTemplatesResult, McpError> {
        Ok(ListResourceTemplatesResult::with_all_items(
            Self::resource_templates(),
        ))
    }

    async fn read_resource(
        &self,
        request: ReadResourceRequestParams,
        _: RequestContext<RoleServer>,
    ) -> std::result::Result<ReadResourceResponse, McpError> {
        let app_uri = app_resource_uri("Inference");
        if request.uri == app_uri {
            let html = default_app_html(
                "telividb inference",
                "0.1.0",
                "Embedding models, and the single compute boundary that serves them.",
            );
            return Ok(ReadResourceResult::new(vec![
                ResourceContents::text(html, request.uri).with_mime_type("text/html"),
            ])
            .into());
        }
        Ok(ReadResourceResult::new(vec![ResourceContents::text("{}", request.uri)]).into())
    }
}

pub const INFERENCE_MCP_DEFAULT_BASE_PATH: &str = "/telividb/inference/v1/inference/mcp";

pub struct InferenceMcpTransportConfig {
    pub transport: String,
    pub host: String,
    pub port: u16,
    pub base_path: String,
}

impl Default for InferenceMcpTransportConfig {
    fn default() -> Self {
        Self {
            transport: "streamable-http".into(),
            host: "0.0.0.0".into(),
            port: 8080,
            base_path: INFERENCE_MCP_DEFAULT_BASE_PATH.into(),
        }
    }
}

pub async fn serve_inference_mcp_stdio<T: InferenceMcpServer>(
    svc: T,
) -> std::result::Result<(), Box<dyn std::error::Error>> {
    let handler = InferenceMcpHandler::new(svc);
    handler
        .serve(rmcp::transport::stdio())
        .await?
        .waiting()
        .await?;
    Ok(())
}

pub async fn serve_inference_mcp<T: InferenceMcpServer>(
    svc: T,
    config: InferenceMcpTransportConfig,
) -> std::result::Result<(), Box<dyn std::error::Error>> {
    let handler = InferenceMcpHandler::new(svc);
    let transports: Vec<&str> = config
        .transport
        .split(',')
        .map(|s| s.trim())
        .filter(|s| !s.is_empty())
        .collect();
    let has_stdio = transports.iter().any(|t| *t == "stdio");
    let http_transport = transports
        .iter()
        .find(|t| **t == "sse" || **t == "streamable-http")
        .copied();

    if let Some(_ht) = http_transport {
        use rmcp::transport::streamable_http_server::{
            StreamableHttpServerConfig, StreamableHttpService, session::local::LocalSessionManager,
        };
        let h = handler.clone();
        let http_config = StreamableHttpServerConfig::default().with_sse_retry(None);
        let http_service = StreamableHttpService::new(
            move || Ok(h.clone()),
            Arc::new(LocalSessionManager::default()),
            http_config,
        );
        let app = axum::Router::new().nest_service(&config.base_path, http_service);
        let addr = format!("{}:{}", config.host, config.port);
        let listener = tokio::net::TcpListener::bind(&addr).await?;
        if has_stdio {
            tokio::spawn(async move {
                axum::serve(listener, app)
                    .await
                    .expect("HTTP server failed");
            });
        } else {
            axum::serve(listener, app).await?;
            return Ok(());
        }
    }

    if has_stdio {
        handler
            .serve(rmcp::transport::stdio())
            .await?
            .waiting()
            .await?;
    } else if http_transport.is_none() {
        return Err(format!("unsupported transport(s): {}", config.transport).into());
    }
    Ok(())
}
